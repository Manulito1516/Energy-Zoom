every image *MUST* of a base two dimensions for maximum compatiblity with hardware acceleration;
this means it can be:
8x8, 16x16, 32x32, 1024x1024, etc

it can also be:
8x128, 512x8, 2048x64, 2x2, 4x2, etc

it can't be any other thing like 13x67 or 56x56